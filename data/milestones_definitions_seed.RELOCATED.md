# Milestone catalog seed — moved

**Canonical path (git):** [`site/public_html/ops/data/milestones_definitions_seed.json`](../site/public_html/ops/data/milestones_definitions_seed.json)

Ops prepare, staging WinSCP, and `scripts/work_prepare` use the ops path so only `public_html/` need be synced.
