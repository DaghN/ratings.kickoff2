# Cursor `move_agent_to_root` — hangs (redirect)

**Archived May 2026** — Cursor/MCP tooling note, not product spec.

**Full write-up:** [`../archive/cursor-move-agent-root-stuck.md`](../archive/cursor-move-agent-root-stuck.md)

**Workaround:** use absolute paths; skip `move_agent_to_root` from empty-window chats.
