# L5 product derived DDL

**Tables:** ratings, standings, catalog stats, matchups, snapshots, generalstats, finish-override table (Tier E — **epistemic L3**; DDL relocate planned).

**Apply:** `apply_schema_derived()` — see `scripts/amiga/schema_bundles.py`.

Policy: [`docs/amiga-data-contract.md`](../../../../docs/amiga-data-contract.md) § derived truth (L5).
