# Ops data (synced with `public_html/ops/`)

| File | Purpose |
|------|---------|
| `milestones_definitions_seed.json` | REP-014 catalog — loaded by `run_prepare.php seed-catalog` |

**Edit catalog** in this file (or regenerate via `scripts/oneoff/` then copy here). Python `work_prepare` reads the same path.
