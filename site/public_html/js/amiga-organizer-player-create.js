/**
 * Amiga tournament organizer — create player compose (nationality picker wired in country-picker.js).
 */
(function () {
    'use strict';

    window.k2OnPageReady(function () {
        /* Round-trip suggest/create handled server-side; reserved for future AJAX preview. */
    });
}());