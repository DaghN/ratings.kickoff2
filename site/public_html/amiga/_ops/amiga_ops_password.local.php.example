<?php
/**
 * Copy to amiga_ops_password.local.php in this folder (gitignored).
 * Staging: WinSCP upload this folder; /config is not writable for dagh SFTP.
 *
 * $admin_password     — import / export
 * $organizer_password — fixtures / live tournament ops
 */
$admin_password = '';
$organizer_password = '';