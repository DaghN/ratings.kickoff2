# Amiga DB dump for staging import

**`ko2amiga_db.sql`** — refreshed by `scripts/export_ko2amiga_db.ps1` before WinSCP sync.

Steve: import this file into MySQL database **`ko2amiga_db`**. Not web-accessible (`.htaccess`).
