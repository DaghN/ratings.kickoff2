; Prod-shaped sandbox DB for Python ladder / sim (gitignored copy: ladder-work.ini).
; PHP: http://work.ratingskickoff.test/ uses ko2unity_work via ko2unitydb_config_work.local.php
; PHP: http://ratingskickoff.test/ uses ko2unity_db via ko2unitydb_config.local.php
;
;   copy ladder-work.ini.example ladder-work.ini
;
; Run replay on work:
;   python -m scripts.ladder run --target sandbox --ini site/config/ladder-work.ini

[database]
host = 127.0.0.1
port = 3306
user = root
password =
database = ko2unity_work
