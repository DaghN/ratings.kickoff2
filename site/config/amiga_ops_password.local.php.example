<?php
/**
 * Amiga ops gate password — local / staging only (gitignored when named *.local.php).
 *
 *   copy amiga_ops_password.local.php.example → amiga_ops_password.local.php
 *
 * Used by:
 *   /amiga/run_import_ko2amiga.php
 *   /amiga/run_export_ko2amiga.php
 *   /amiga/ops/fixtures.php
 *   scripts/lib/Pull-Ko2AmigaStaging.ps1 (reads this file)
 *
 * Docs use &pwd=YOUR_OPS_PASSWORD — never commit the real value.
 */
$password = '';