<?php
/**
 * Amiga ops passwords — local / staging only (gitignored when named *.local.php).
 *
 *   copy amiga_ops_password.local.php.example → amiga_ops_password.local.php
 *
 * $admin_password     — /amiga/run_import_ko2amiga.php, /amiga/run_export_ko2amiga.php
 * $organizer_password — /amiga/ops/fixtures.php (admin password also accepted)
 *
 * Prefer POST password forms (no ?pwd= in the address bar).
 * Legacy single $password still works for both roles if the new keys are empty.
 */
$admin_password = '';
$organizer_password = '';